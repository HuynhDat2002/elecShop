import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false,
   images: {
    domains: ['ongthotao.com','encrypted-tbn0.gstatic.com'],
  },
};

export default nextConfig;
